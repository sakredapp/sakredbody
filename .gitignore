node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz
.env
.env.local
.env.production
.vercel
# drizzle-kit scratch output (schema is managed via db:push / supabase/*.sql)
migrations/

# Xcode per-user state inside the local plugin — machine-specific, not shared.
plugins/**/.swiftpm/

# Gradle output from the local plugin module.
plugins/**/build/
