
/**
 * Stamped by vite.config.ts at build time. See the note there: the native
 * shells bundle the client, so this is the only reliable way to know what a
 * given phone is actually running.
 */
declare const __BUILD_SHA__: string;
declare const __BUILT_AT__: string;
